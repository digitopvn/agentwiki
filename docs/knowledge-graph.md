# Knowledge Graph

Dual-layer knowledge graph enabling typed document relationships, semantic discovery, and AI-powered graph reasoning.

## Architecture Overview

```
                        ┌──────────────────────────────────┐
                        │         Frontend (/graph)         │
                        │   Cytoscape.js + AI Insight Panel │
                        └───────────────┬──────────────────┘
                                        │ REST API
                        ┌───────────────▼──────────────────┐
                        │        Graph API (7 endpoints)    │
                        │   graph.ts → graph-service.ts     │
                        └──────┬────────────────┬──────────┘
                               │                │
              ┌────────────────▼───┐    ┌───────▼──────────────┐
              │  Layer 1: Explicit  │    │  Layer 2: Implicit    │
              │  (Typed Wikilinks)  │    │  (Semantic Similarity)│
              │                     │    │                       │
              │  document_links     │    │  document_similarities│
              │  6 edge types       │    │  Vectorize cosine     │
              │  D1 SQLite          │    │  Top-5 cached/doc     │
              └────────┬───────────┘    └───────┬──────────────┘
                       │                        │
              ┌────────▼────────────────────────▼──────────────┐
              │            AI Auto-Organization                 │
              │  Queue jobs → Workers AI (Llama 3.1 8B)        │
              │  • Infer edge types from link context           │
              │  • Compute similarities via Vectorize           │
              └────────────────────────────────────────────────┘
```

## Dual-Layer Design

### Link Extraction

**Two extraction methods** are combined via `extractAllLinks()`:

- **Wikilinks** — regex: `\[\[([^\]]+)\]\]` — extracts `[[target]]` with optional `|type:edge-type` annotations
- **Markdown links** — regex: `\[([^\]]*)\]\(\/doc\/([^)]+)\)` — extracts standard `[text](/doc/slug)` patterns

Both formats are deduplicated by target (wikilinks take priority to preserve type info). Extraction occurs on:
- Document creation (`POST /api/documents`)
- Document update (`PATCH /api/documents/:id`)
- Backfill operation (`POST /api/graph/backfill-edges`)

### Layer 1: Explicit Edges (Typed Wikilinks & Markdown Links)

Documents are connected through two link formats:

1. **Wikilinks** `[[target]]` — carry optional type annotations
2. **Markdown links** `[text](/doc/slug)` — standard markdown format

Each link creates an **explicit typed relationship**:

| Edge Type | Meaning | Example |
|-----------|---------|---------|
| `relates-to` | General relationship (default) | `[[API Design]]` or `[API Design](/doc/api-design)` |
| `depends-on` | Source requires target | `[[Auth Module\|type:depends-on]]` (wikilinks only) |
| `extends` | Source builds upon target | `[[Base Config\|type:extends]]` |
| `references` | Source cites target | `[[RFC 7519\|type:references]]` |
| `contradicts` | Source conflicts with target | `[[Old Approach\|type:contradicts]]` |
| `implements` | Source implements target concept | `[[Design Spec\|type:implements]]` |

**Wikilink syntax** (supports type annotations):

```markdown
[[Target Page]]                          → simple link (relates-to)
[[Target Page|Display Text]]             → link with custom display text
[[Target Page|type:depends-on]]          → typed link
[[Target Page|Display Text|type:extends]] → typed link with display text
```

**Markdown link syntax** (no type annotations; always relates-to):

```markdown
[Display Text](/doc/target-slug)         → standard markdown link
[](/doc/target-id)                       → link with no display text
```

**Storage**: `document_links` table in D1 with columns: `source_doc_id`, `target_doc_id`, `type` (default `relates-to`), `weight`, `inferred`, `context`.

### Layer 2: Implicit Edges (Semantic Similarity)

Documents with similar content are automatically connected via vector embeddings:

```
Document A ──embed──▶ Vectorize ◀──embed── Document B
    │                    │                      │
    └────── cosine similarity score ────────────┘
                   (0.0 → 1.0)
```

- **Embeddings**: Generated by Workers AI (`bge-base-en-v1.5`) on document creation/update
- **Similarity query**: Vectorize `query()` returns top-K nearest neighbors
- **Cache**: `document_similarities` table stores top-5 results per document (threshold ≥ 0.7)
- **Refresh**: Async Queue job `compute-similarities` re-computes on document changes

## Data Flow

```
┌──────────────┐     save      ┌──────────────┐    extract     ┌──────────────┐
│   User edits │ ──────────▶  │  Document    │ ──────────▶  │  Wikilink    │
│   document   │              │  Service      │              │  Extractor   │
└──────────────┘              └──────┬───────┘              └──────┬───────┘
                                     │                              │
                                     │ queue                        │ sync links
                                     ▼                              ▼
                              ┌──────────────┐              ┌──────────────┐
                              │  Cloudflare  │              │ document_    │
                              │  Queue       │              │ links (D1)   │
                              └──────┬───────┘              └──────────────┘
                                     │
                        ┌────────────┼────────────┐
                        ▼                          ▼
                 ┌──────────────┐          ┌──────────────┐
                 │  compute-    │          │  infer-edge- │
                 │  similarities│          │  types       │
                 │  (Vectorize) │          │  (Workers AI)│
                 └──────┬───────┘          └──────┬───────┘
                        ▼                          ▼
                 ┌──────────────┐          ┌──────────────┐
                 │ document_    │          │ document_    │
                 │ similarities │          │ links.type   │
                 │ (D1 cache)   │          │ (D1 update)  │
                 └──────────────┘          └──────────────┘
```

## API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/api/graph` | Full graph (nodes + typed edges), filter by category/tag/type |
| `GET` | `/api/graph/neighbors/:id` | N-hop neighbors (1-3 depth), direction filter |
| `GET` | `/api/graph/subgraph/:id` | Ego-network centered on a document |
| `GET` | `/api/graph/path/:from/:to` | Shortest path between two documents (BFS) |
| `GET` | `/api/graph/stats` | Graph statistics (density, degree distribution, orphans) |
| `GET` | `/api/graph/similar/:id` | On-demand Vectorize similarity query |
| `GET` | `/api/graph/suggest-links/:id` | AI-suggested missing links |
| `POST` | `/api/graph/backfill-edges` | Re-process all existing documents to extract edges (admin only) |

**Backfill endpoint usage** (admin only, requires `org:manage` permission):
```bash
POST /api/graph/backfill-edges
Response: { "ok": true, "documentsProcessed": 42, "edgesCreated": 157 }
```
Useful when link extraction logic changes or to re-process legacy content.

**Common query parameters:**
- `types` — Comma-separated edge types to filter: `?types=depends-on,extends`
- `include_implicit` — Merge similarity edges: `?include_implicit=true`
- `depth` — Traversal depth (1-3): `?depth=2`
- `max_nodes` — Limit result size: `?max_nodes=100`

## MCP Tools (AI Agent Integration)

7 tools expose graph reasoning to AI agents:

| Tool | Purpose | Use Case |
|------|---------|----------|
| `graph_get` | Full graph with type filters | "Show me all dependency relationships" |
| `graph_traverse` | Multi-hop traversal | "What does Auth Module depend on?" |
| `graph_find_path` | Shortest path | "How are Design Spec and API connected?" |
| `graph_suggest_links` | Link recommendations | "What docs should link to this one?" |
| `graph_explain_connection` | Relationship explanation | "Why are these two docs related?" |
| `graph_stats` | Graph analytics | "How healthy is our knowledge base?" |

**Example agent interaction:**
```
User: What documents depend on the Auth Module?
Agent: [calls graph_traverse with edgeTypes=["depends-on"], direction="inbound"]
→ Found 4 documents that depend on Auth Module:
  - Login Page (depends-on, 1 hop)
  - API Gateway (depends-on, 1 hop)
  - User Settings (depends-on → Login Page → Auth Module, 2 hops)
  - Admin Dashboard (depends-on, 1 hop)
```

## Frontend Visualization

The `/graph` page renders an interactive force-directed graph:

```
┌─────────────────────────────────────────────────────────────┐
│ ┌─────────────────────┐  ┌──────────────────────────────┐  │
│ │   Filter Toolbar     │  │      AI Insight Panel        │  │
│ │ [Edge Types ▼]       │  │                              │  │
│ │ [Category ▼]         │  │  Overview                    │  │
│ │ [☐ Show Implicit]    │  │  Nodes: 142  Edges: 387      │  │
│ └─────────────────────┘  │  Avg Degree: 5.4              │  │
│                           │  Orphans: 3                   │  │
│  ┌─────────────────────┐ │                              │  │
│  │                     │ │  Top Connected                │  │
│  │    Cytoscape.js     │ │  1. API Reference (23)       │  │
│  │   Force-Directed    │ │  2. Auth Module (18)         │  │
│  │     Graph Canvas    │ │  3. Getting Started (15)     │  │
│  │                     │ │                              │  │
│  │   ● Node A         │ │  Similar Documents           │  │
│  │   │ depends-on     │ │  (click a node to see)       │  │
│  │   ● Node B         │ │                              │  │
│  │   ┊ similarity     │ │  Path Between Nodes          │  │
│  │   ○ Node C         │ │  (select 2 nodes to see)     │  │
│  │                     │ │                              │  │
│  └─────────────────────┘ └──────────────────────────────┘  │
│                                                             │
│  Legend                                    [+] [-] [⊡]     │
│  ── relates-to  ── depends-on              Zoom controls    │
│  ── extends     -- references                               │
│  -- contradicts ── implements                               │
│  ·· similarity                                              │
└─────────────────────────────────────────────────────────────┘
```

**Visual encoding:**
- **Node size** scales with degree (more connections = larger)
- **Node color** maps to document category (guide=blue, reference=purple, tutorial=green, api=amber, concept=pink)
- **Edge color** maps to type (depends-on=blue, extends=green, contradicts=red, etc.)
- **Edge style**: solid=explicit, dashed=references/contradicts, dotted=implicit similarity
- **Interactions**: Click=select, Shift+click=multi-select, Double-click=navigate, Scroll=zoom, Drag=pan

## Database Schema

```sql
-- Explicit edges (typed wikilinks)
CREATE TABLE document_links (
  id            TEXT PRIMARY KEY,
  source_doc_id TEXT NOT NULL REFERENCES documents(id),
  target_doc_id TEXT NOT NULL REFERENCES documents(id),
  context       TEXT,           -- surrounding text for preview
  type          TEXT NOT NULL DEFAULT 'relates-to',
  weight        REAL DEFAULT 1.0,
  inferred      INTEGER DEFAULT 0,  -- 0=explicit, 1=ai-inferred
  created_at    INTEGER NOT NULL
);

-- Implicit edges (semantic similarity cache)
CREATE TABLE document_similarities (
  id            TEXT PRIMARY KEY,
  source_doc_id TEXT NOT NULL REFERENCES documents(id),
  target_doc_id TEXT NOT NULL REFERENCES documents(id),
  score         REAL NOT NULL,  -- cosine similarity 0-1
  computed_at   INTEGER NOT NULL,
  UNIQUE(source_doc_id, target_doc_id)
);
```

## Key Implementation Files

| Package | File | Purpose |
|---------|------|---------|
| shared | `types/graph.ts` | EdgeType enum, GraphNode/Edge/Response interfaces |
| api | `services/graph-service.ts` | BFS traversal, full graph queries, analytics |
| api | `services/similarity-service.ts` | Vectorize integration, cache, on-demand queries |
| api | `services/graph-ai-service.ts` | AI edge type inference via Workers AI |
| api | `services/document-service.ts` | `syncWikilinks()` — extracts both wikilinks + markdown links |
| api | `routes/graph.ts` | 8 REST endpoints (including backfill) |
| api | `utils/wikilink-extractor.ts` | Extract `[[target\|type:X]]` wikilinks + `[text](/doc/slug)` markdown links |
| api | `queue/handler.ts` | `compute-similarities` + `infer-edge-types` jobs |
| mcp | `tools/graph-traversal-tools.ts` | 7 MCP tools for AI agents |
| web | `components/graph/graph-canvas.tsx` | Cytoscape.js wrapper |
| web | `components/graph/graph-insight-panel.tsx` | Stats, similar docs, path finder |
| web | `hooks/use-graph.ts` | React Query hooks for graph data |

## Performance Characteristics

| Operation | Target | Approach |
|-----------|--------|----------|
| Full graph render | < 1s for 1K nodes | Cytoscape.js with `cose` layout |
| Neighbor query (2-hop) | < 200ms | In-memory BFS on tenant graph |
| Shortest path | < 200ms for 5K docs | BFS with adjacency list |
| Similarity query | < 500ms | Vectorize `query()` + D1 metadata |
| Edge type inference | ~2s per link | Workers AI Llama 3.1 8B, batched (10/job) |

## Migration Notes

**Markdown link support** (2026-03-27):
- `extractInternalLinks()` now extracts `[text](/doc/slug)` format
- Markdown links treated as `relates-to` edges (no type annotations in markdown format)
- Existing markdown links in documents are automatically detected and indexed
- To re-process all documents after this fix, call `POST /api/graph/backfill-edges`
- Deduplication: if both wikilink and markdown link target same document, wikilink is retained

## Known Limitations

- BFS loads full tenant graph into memory (acceptable for < 1K docs per tenant)
- Similarity threshold hardcoded at 0.7 (configurable in future)
- Path finding capped at 10 hops maximum
- Edge type inference batched at 10 links per queue job (Workers AI quota)
- Graph visualization optimized for < 10K nodes
- Markdown links cannot carry type annotations (use wikilinks with `|type:X` for typed relationships)
